#include <stdio.h>

int main() {
    int m1,m2,m3,m4,m5,total,percentage,pen=0;
    printf("enter the marks of maths:");
    scanf("%d",&m1);
    printf("enter the marks of english:");
    scanf("%d",&m2);
    printf("enter the marks of chemistry:");
    scanf("%d",&m3);
    printf("enter the marks of physics:");
    scanf("%d",&m4);
    printf("enter the marks of computer:");
    scanf("%d",&m5);
    total = m1+m2+m3+m4+m5;
    percentage=total*100/500;
    printf("total marks:%d\n",total);
    printf("percentage obtain:%d\n",percentage);
    if(m1||m2||m3||m4||m5<18)
    {
        printf("fail\n");
        if(m1<18)
        {
            printf("you are fail in maths\n");
            pen+=500;
        }
        if(m2<18)
        {
            printf("you are fail in english\n");
            pen+=500;
        }
        if(m3<18)
        {
            printf("you are fail in chemistry\n");
            pen+=500;
        }
        if(m4<18)
        {
            printf("you are fail in physics\n");
            pen+=500;
        }
        if(m5<18)
        {
            printf("you are fail in computer\n");
            pen+=500;
        }
        printf("total penalty is:%d\n",pen);
    }
    else
    {
        if(percentage>80 && percentage<=100)
        {
            printf("GRADE A+\n");
        }
        else if(percentage>60 && percentage<=80)
        {
            printf("GRADE B\n");
        }
        else if(percentage>35 && percentage<=60)
        {
            printf("GRADE C\n");
        }
    }
    return 0;
}


output

enter the marks of maths:84
enter the marks of english:83
enter the marks of chemistry:87
enter the marks of physics:10
enter the marks of computer:92
total marks:356
percentage obtain:71
fail
you are fail in physics
total penalty is:500
